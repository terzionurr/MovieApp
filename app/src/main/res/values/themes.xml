<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/Background</item>
        <item name="android:statusBarColor">@color/Background</item>
    </style>

    <style name="Theme.MovieApp" parent="Base.Theme.MovieApp" />
    <style name="MovieInfoTextStyle">
        <item name="android:textSize">@dimen/info_text_size</item>
        <item name="android:textColor">@color/item_view_color</item>
    </style>
</resources>